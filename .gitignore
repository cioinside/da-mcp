node_modules/
dist/
coverage/
*.log
.DS_Store
.env
.env.local
tessdata/
.tess-cache/

# Bundled SEA assets — the traineddata binary is committed (sea build inlines
# it via --define); ignore everything else under assets/.
assets/*
!assets/*.traineddata
!/tmp/da-mcp-*.png
/tmp/da-mcp-*.json
/tmp/da-mcp-*.jsonl
/tmp/da-mcp-manual-qa/

# Node SEA (Single Executable Application) artifacts
dist-sea/
sea-config.json
# Bundled SEA binary at repo root (built by `npm run build:sea`).
# Without this, `da-mcp upgrade` refuses to run on a dirty working tree.
da-mcp.exe
da-mcp.exe.*

# Tooling / runtime artifacts (not project source)
.omo/
.opencode/
.codegraph/
.codegraph
.local/